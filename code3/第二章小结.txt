1、计算机软件开发使用的抽象有过程抽象和数据抽象两种：
(1)过程抽象强调功能完成的过程和步骤
(2)数据抽象，每个抽象数据类型包含了数据和针对这些数据的操作
-->数据抽象与面向对象程序设计

2、Java的封装性是通过访问控制权限实现的。
-->封装与访问控制权限

3、在创建某类的对象时应该：
先声明对象，为对象分配内存空间，再初始化对象，然后才能使用对象

4、枚举是一种特殊的类enum，因为它的对象的个数是有限的，枚举值是类的对象，
枚举中可以定义数据和方法，枚举类的values()方法用于获得枚举类型的枚举值的数组，
private的构造函数

5、DecimalFormat类在java.text包中， new java.text.DecimalFormat("$0.00").format(balance);
